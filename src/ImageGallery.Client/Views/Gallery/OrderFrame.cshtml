@model ImageGallery.Client.ViewModels.OrderFrameViewModel

<div class="container">
    <div class="h3 bottomMarginDefault">Order a framed version of your picture</div>
    <div class="bottomMarginSmall">We have got this address on the record for you</div>

    <div class="text text-info bottomMarginSmall">@Model.Address</div>

    <div class="text">If this isn't a correct adrres then call us</div>

</div>